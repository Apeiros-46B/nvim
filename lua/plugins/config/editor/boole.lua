-- configuration for boole.nvim plugin
require('boole').setup({
    -- user-defined
    additions = {},
    mappings = {},
    allow_caps_additions = {
        { 'enable', 'disable' }
        -- enable -> disable
        -- Enable -> Disable
        -- ENABLE -> DISABLE
    }
})
