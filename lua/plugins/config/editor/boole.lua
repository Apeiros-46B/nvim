-- configuration for boole.nvim plugin
-- {{{ setup
require('boole').setup({
    additions = { -- user-defined

    },
    allow_caps_additions = {
        { 'enable', 'disable' }
        -- enable -> disable
        -- Enable -> Disable
        -- ENABLE -> DISABLE
    }
})
-- }}}
